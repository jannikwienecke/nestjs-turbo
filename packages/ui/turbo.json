{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "pipeline": {
    "build": {
      "outputs": ["dist/**"]
    },
     "dev": {
      "dotEnv": [".env.local", ".env.local", ".env.development", ".env"]
    }
  }
}
